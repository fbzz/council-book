"""Mechanical reference book (trend level x vol-capped unit weight) and its daily backtest.

The reference is the council's default position, band centre, fallback and primary benchmark.
It never shorts and never levers: aggression comes from composition at about 1x gross.
"""
