"""Single US stocks: the point-in-time fundamentals (`pit`), the four-feature score and selection
(`score`) and the SIC to Fama-French 12 sector map (`sectors`) that the pre-registered stock-sleeve
study tests (docs/stock-sleeve-spec.md, tag `stock-sleeve-spec`). All three modules are frozen at that
tag; the live quarterly rank imports them, it never re-implements them.
"""
